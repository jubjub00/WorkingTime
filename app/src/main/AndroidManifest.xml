<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.workingtimewfh">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".ui.admin.home_admin.DetailEmployees"></activity>
        <activity
            android:name=".WorkingList"
            android:label="@string/title_activity_working_list" />
        <activity
            android:name=".MenuUser"
            android:label="@string/title_activity_menu_user"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".MenuAdmin"
            android:label="MenuAdmin"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.user.leave.add_leave" />
        <activity android:name=".ui.user.leave.view_leave" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>